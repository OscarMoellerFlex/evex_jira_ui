streamlit==1.56.0
pandas==2.3.3
numpy==2.5.2
plotly==7.0.0
pytz==2026.3.post1

# JIRA fetch
jira==3.10.5
python-dotenv==1.2.3

# UI widgets
streamlit-aggrid==1.2.1.post2
pygwalker==0.5.0.1
pyarrow==25.0.1

# Holidays
holidays==0.103

# Transitive floors. osv-scanner resolves transitive dependencies to their
# *minimum* satisfying version, which reported CVEs in pillow/gitpython/idna
# that a real install never receives (pip/uv resolve 12.3.0 / 3.1.62 / 3.19).
# Pinning the floors makes the scan accurate and the image reproducible.
pillow>=12.3.0
gitpython>=3.1.62
idna>=3.19
